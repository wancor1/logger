[project]
name = "wanlogger"
version = "1.0.1"
description = "original logger!"
authors = [{name = "wancoro_inu"}]
dependencies = [
    "ntplib",
    "rich"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"