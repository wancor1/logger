[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wanlogger"
version = "1.1.2"
description = "verbose logger."
authors = [{name="wancoro_inu"}]
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
    "ntplib",
    "rich"
]
